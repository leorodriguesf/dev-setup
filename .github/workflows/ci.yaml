name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Start development setup
        run: |
          DEV_SETUP_REF=${{ github.head_ref }} ./boot
